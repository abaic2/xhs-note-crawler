# `playwright` 只是 Python 驱动，浏览器本体来自 packages.txt 里的 apt chromium
# （云端不能用 playwright install —— 它下载的内核在这个环境里跑不起来）。
streamlit>=1.40,<2
playwright>=1.40,<2
jieba>=0.42
requests>=2.31
# 用来把内嵌进报告的图片缩小（否则 18 张原图能让报告到 6.6MB，
# 传输慢且容易失败）。缺了也能跑，只是报告会大很多。
pillow>=10
